import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.web.watfordevents',
  appName: 'Watford Events',
  webDir: 'dist/watford-events/browser',
  // server: {
  //   androidScheme: 'https'
  // }
};

export default config;
