import packageInfo from '../../../../package.json';

export const APP_VERSION = packageInfo.version;
export const APP_NAME = packageInfo.name;
// export const APP_DESCRIPTION = packageInfo.description;
